# فانوس (Fanous)

> عالمٌ حيّ ينبض داخل فانوس سحري — ينمو معك، ويتغيّر باستمرار، ولا ينتهي أبدًا.

لعبة أندرويد أصلية (APK)، مبنية بالكامل داخل هذا المستودع، تُصدَّر تلقائيًا
عبر GitHub Actions بلا أي خطوة يدوية. الفكرة الكاملة، ولماذا هي مختلفة عن أي
لعبة موجودة حاليًا على متاجر الهواتف، موثّقة في
[`docs/GAME_DESIGN.md`](docs/GAME_DESIGN.md). القرارات التقنية وأسبابها
موثّقة في [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md).

هذا الملف يغطي الجانب العملي فقط: ما هو موجود، وكيف يعمل، وكيف تشغّله.

## لمحة سريعة

| | |
|---|---|
| **المحرك** | [Godot 4.7.1](https://godotengine.org) (Forward+ / Vulkan) |
| **لغة البرمجة** | GDScript، بتوصيف أنواع صارم (Static Typing) |
| **المنصة** | أندرويد فقط، عمودي (Portrait)، للمس بالكامل |
| **البناء** | GitHub Actions → APK جاهز للتثبيت كمُخرَج (Artifact) في كل push |
| **الواجهة** | عربية بالكامل، RTL أصيل عبر نظام Godot الداخلي |
| **الترخيص** | Godot نفسه MIT (لا قيود). كود اللعبة ومحتواها ملكية خاصة — لا يوجد ملف LICENSE مفتوح المصدر عن قصد؛ راجع القسم أدناه |

## حالة المشروع

اكتملت **المرحلتان 1 و2**: البنية التحتية الكاملة، ونظام الواجهة العربية
الكامل. المشروع الآن يبني وينشر تطبيقًا حقيقيًا وقابلًا للتثبيت: شاشة
إقلاع متحركة → قائمة رئيسية عربية كاملة بثلاث شاشات فعلية (رئيسية،
إعدادات تعمل فعليًا وتتحكم بالصوت والجودة، وعن اللعبة)، بخط عربي احترافي
حقيقي وأنيميشن ناعم على كل زر. لا يوجد بعد عالم لعب فعلي (زر "لعب" ظاهر
لكن معطَّل بوضوح) — هذا يأتي في المرحلة 3 الموضحة في خارطة الطريق أسفل
هذا الملف.

كل ملف مذكور هنا **يعمل فعليًا** وتم التحقق منه محليًا عبر تشغيل حقيقي
لمحرك Godot 4.7.1 (استيراد المشروع كاملًا، تشغيل القائمة الرئيسية فعليًا
والتحقق من عمل كل زر وشريحة، وتشغيل خط تصدير Android حتى آخر خطوة ممكنة
دون SDK فعلي) — وليس كودًا مكتوبًا نظريًا فقط.

## بنية المستودع

```
project.godot                  # إعدادات المشروع الرئيسية
export_presets.cfg             # إعداد تصدير Android
icon.svg                       # أيقونة المشروع (محرر Godot)

.github/workflows/
  build-android.yml            # خط أنابيب CI/CD الكامل

assets/icons/                  # أيقونة التطبيق بكل الأحجام/الطبقات المطلوبة لأندرويد
assets/fonts/cairo/            # خط الواجهة (Cairo، ترخيص OFL)
assets/fonts/lalezar/          # خط العنوان (Lalezar، ترخيص OFL)
assets/textures/               # نسيج الجسيمات المُولَّد بالكود

theme/
  fanous_theme.tres            # الثيم المركزي — كل ألوان/أنماط الواجهة

audio/
  bus_layout.tres              # قنوات الصوت: Master/Music/SFX/UI/Ambience

scripts/autoload/               # الأنظمة العامة (Singletons) — راجع ARCHITECTURE.md
  event_bus.gd
  game_manager.gd
  scene_manager.gd
  save_manager.gd
  settings_manager.gd
  audio_manager.gd

scripts/ui/
  touch_button_feedback.gd     # نبضة لمس ناعمة قابلة لإعادة الاستخدام لأي زر

scenes/boot/                    # مشهد الإقلاع
  boot.tscn
  boot.gd

scenes/main_menu/               # القائمة الرئيسية (رئيسية/إعدادات/عن اللعبة)
  main_menu.tscn
  main_menu.gd

docs/
  ARCHITECTURE.md               # القرارات التقنية ولماذا اتُّخذت
  GAME_DESIGN.md                # وثيقة تصميم اللعبة الكاملة
```

## كيف يعمل البناء التلقائي

كل push إلى فرع `main` يُشغّل `.github/workflows/build-android.yml`، الذي:

1. يجهّز JDK 17 وAndroid SDK على عامل Ubuntu نظيف.
2. يُنزّل محرر Godot 4.7.1 headless وقوالب التصدير المطابقة (ومخزَّنة مؤقتًا
   Cache للتشغيلات اللاحقة كي لا يُعاد تنزيلها كل مرة).
3. يستورد كل موارد المشروع مرة واحدة (خطوة ضرورية لتفادي مشكلة معروفة في
   Godot عند التصدير من مستودع نظيف — مشروحة داخل ملف الـworkflow نفسه).
4. يُثبّت قالب بناء Android عبر Gradle، ثم يُصدّر نسخة APK للتصحيح (Debug).
5. يتحقق فعليًا من وجود ملف الـAPK الناتج (وليس فقط من رمز خروج الأمر).
6. يرفع الـAPK كمُخرَج (Artifact) قابل للتنزيل من تبويب Actions في GitHub،
   لمدة 30 يومًا.

لا حاجة لأي Secrets أو إعداد يدوي في هذه المرحلة — نسخة Debug تُستخدم مفتاح
تصحيح Godot الافتراضي المدمج تلقائيًا. توقيع نسخة Release الحقيقية لمتجر
Google Play سيُضاف في مرحلة لاحقة عبر GitHub Secrets، بدون تعديل جوهري في
الملف الحالي.

### تنزيل وتثبيت آخر بناء

من تبويب **Actions** في المستودع → افتح آخر تشغيل ناجح لـ *Build Android
APK* → مُخرَج `fanous-debug-apk` أسفل الصفحة → نزّل الملف المضغوط، واستخرج
`fanous-debug.apk`، وثبّته على جهاز أندرويد (يتطلب تفعيل "تثبيت من مصادر
غير معروفة" لنسخ التصحيح غير الموقّعة بمفتاح متجر رسمي).

## التشغيل محليًا (اختياري)

لست مضطرًا لفتح Godot محليًا لأي شيء — كل شيء يعمل عبر GitHub. لكن إن أردت
المعاينة والتعديل المباشر:

1. نزّل [Godot 4.7.1](https://godotengine.org/download) (النسخة القياسية،
   ليست Mono/.NET — المشروع GDScript خالص).
2. افتح `project.godot` من داخل Godot مباشرة.
3. F5 للتشغيل على سطح المكتب لمعاينة سريعة، أو Project → Export لتصدير
   محلي على أندرويد (يتطلب إعداد Android SDK محليًا حسب
   [توثيق Godot الرسمي](https://docs.godotengine.org/en/stable/tutorials/export/exporting_for_android.html)).

## ملاحظة حول الترخيص

Godot نفسه مرخّص بموجب [MIT](https://godotengine.org/license/)، ما يعني لا
قيود إطلاقًا ولا رسوم إتاوة على استخدامه تجاريًا — وهذا أحد أسباب اختياره
لمشروع تجاري (راجع `docs/ARCHITECTURE.md`). هذا المستودع نفسه **لا يحمل
ملف LICENSE مفتوح المصدر عن قصد**: كل الحقوق محفوظة افتراضيًا لصاحب
المشروع، وهو الوضع الصحيح للعبة تجارية سيُطلقها فريقها عالميًا. لا تُضِف
ملف LICENSE من نوع MIT/Apache/GPL هنا إلا إذا قررتم فعليًا فتح مصدر اللعبة
— إضافته بلا قصد تعني التنازل عن حقوق الملكية على الكود.

## خارطة الطريق

- [x] **المرحلة 1** — الهيكل المعماري، الأنظمة العامة، خط CI/CD كامل
- [x] **المرحلة 2** — نظام الواجهة العربية (خط مخصص، RTL، الثيم المركزي، القائمة الرئيسية)
- [ ] **المرحلة 3** — عرض الرسوميات: بيئة الفانوس ثلاثية الأبعاد، PBR،
      الإضاءة الديناميكية، التوهّج، الانعكاسات
- [ ] **المرحلة 4** — أنظمة اللعب الأساسية: التحكم باللمس، النحت التفاعلي
      للتضاريس، حفظ حالة العالم
- [ ] **المرحلة 5** — محاكاة الإيكولوجي: الأنواع، الوراثة، التطور عبر الأجيال
- [ ] **المرحلة 6** — الأحداث المتجددة والتقويم الفلكي المرتبط بالوقت الحقيقي
- [ ] **المرحلة 7** — الإنجازات، المكافآت، والعناصر الاجتماعية غير المتزامنة
- [ ] **المرحلة 8** — تحسين الأداء والصقل النهائي قبل الإطلاق

تفاصيل كل مرحلة موجودة في `docs/GAME_DESIGN.md`.
